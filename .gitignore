.testrepos/
.codenotes/hook.log
__pycache__/
.codenotes/hook.log
.codenotes/state.json

# 기계마다 다른 절대 경로가 들어간다. codenotes init 이 만든다.
.claude/settings.json

# note의 why는 모델이 편집 직전에 쓴 말이다. 그대로 커밋하면 대화 내용이 밖에 나간다.
# 공유하려면 codenotes init --shared 를 보고 이 줄을 지운다.
.codenotes/**/*.jsonl
